{% extends 'admin/base_admin.html.twig' %}

{% block body %}
    <!-- DataTales Example -->
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <a href="{{ path('admin_slide') }}"> <i class="fa fa-backward"></i> Revenir dans la liste</a>
        </div>
        <div class="card-body">
           {{ form_start(form) }}
                {{ form_widget(form) }}
            <input type="submit" class="btn btn-primary" value="Enregistrer">
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}